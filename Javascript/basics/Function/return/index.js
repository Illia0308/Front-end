//let number = 10;
//let chickens = number * 0.2;
//let cows = number * 1.5;
//let pigs = number * 0.5;
//let countArea = cows + pigs + chickens;
//console.log(countArea);

function countArea(chickens, cows, pigs) {
  return chickens * 0.2 + cows * 1.5 + pigs * 0.5;
}
console.log(countArea(10, 10, 10));
